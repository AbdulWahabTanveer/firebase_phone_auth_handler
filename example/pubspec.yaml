name: phone_auth_handler_demo
description: Demo for the package firebase_phone_auth_handler on pub.dev

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.1.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  pinput: ^3.0.1
  firebase_core: ^2.24.0
  easy_container: ^1.0.4+1
  intl_phone_field: ^3.2.0
  firebase_phone_auth_handler:
    path: "../"

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1


flutter:
  uses-material-design: true